name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: kennylouie/setup-ops@v1
    - name: tests
      run: |
        echo "Running tests..."
        make test
        echo "Done!"
